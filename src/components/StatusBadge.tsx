// Deprecated: StatusBadge has been removed as product status is no longer used.
// Keeping a no-op export to avoid import errors if referenced accidentally.
export default function StatusBadge() {
  return null
}
